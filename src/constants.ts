/**
 * The key-value separator used in serialization.
 */
export const KEY_VALUE_SEPARATOR = ":" as const;

/**
 * The entry separator used in serialization.
 */
export const ENTRY_SEPARATOR = "," as const;
